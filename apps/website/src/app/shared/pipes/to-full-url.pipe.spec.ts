import { ToFullUrlPipe } from './to-full-url.pipe';

describe('ToFullUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ToFullUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
